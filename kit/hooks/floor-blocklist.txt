# Floor guard — moves that LOWER the quality bar on the way to a green check (ERE, case-SENSITIVE).
#
# An agent that hits a red check does not invent a clever loophole; it takes the cheapest road to green. That road
# is nearly always one of a handful of shapes, and every one of them is visible in the lines a commit ADDS: a
# checker switched off where it fired, a test made to stop running, a stub or a swallowed error standing where the
# work should be. Tightening the bar needs no gate. Loosening it should be loud. This list is the loud part.
#
# Only ADDED lines in project code are scanned. Documentation (.md .mdx .txt .rst .adoc) is not: a suppression
# written in prose silences nothing. Generated files are not either — a file whose first lines carry a generator
# marker (`<auto-generated`, `Code generated … DO NOT EDIT`, `@generated`) was not written by the author of this
# commit, and EF Core alone puts a warning pragma in every model snapshot it emits (395k such files on GitHub,
# measured), so without that exemption an EF Core project could not commit a migration.
#
# A legitimate exception is a tracked decision, not a loosened rule: `.floor-allowlist.txt` at the repo root, one
# entry per line — `path:<glob>` exempts a path, `rule:<name>` turns one rule off, and any other line exempts the
# EXACT pattern line below. The allowlist change sits in the same diff as the code it excuses, where review sees it.
#
# `path:` IS A SHELL PATTERN, NOT A .gitignore GLOB, and it is wider than it reads. `*` also matches `/`, so
# `path:*.cs` exempts EVERY C# file in the repository, not the ones at the root; and `**` means nothing special, so
# `path:**/gen/*` exempts `a/gen/x.cs` but not a root-level `gen/x.cs`. Both were measured on Windows 11 and both err
# toward exempting more than the author meant, which is the dangerous direction for an exception. Write the narrowest
# path that names the thing: `path:src/legacy/*`, not `path:*legacy*`.
#
# Each pattern carries its own case below it (`#test:` must be caught, `#test-clean:` must not), and smoke-test.sh
# drives every one through the REAL pre-commit. A `# rule:` line names the rule for the patterns that follow it.
#
# A PATTERN MUST NOT BEGIN WITH `#`. Every loop that reads this file treats such a line as a comment and skips
# it, so `#pragma …` or `# noqa` written plainly is a pattern that looks armed and matches nothing. Write the
# character as a one-member bracket instead — `[#]pragma` is the same ERE and is read as a pattern.
#
# Adapted from the "floor" in addyosmani/agent-skills (constraint-driven-development, MIT). Rewritten for this kit:
# bash instead of Node, the staged diff instead of a merge base, per-ecosystem patterns for the stacks this kit
# supports, and the generated-file and documentation exemptions that stack needs.

# rule: silenced-checker
@ts-(ignore|nocheck)
#test: const total = compute(order); // @ts-ignore
#test: /* @ts-nocheck */
eslint-disable
#test: // eslint-disable-next-line no-explicit-any
biome-ignore
#test: // biome-ignore lint/suspicious/noExplicitAny: legacy
(istanbul|c8) ignore
#test: /* istanbul ignore next */
[#][[:space:]]*noqa
#test: import os, sys  # noqa: E401
[#][[:space:]]*type:[[:space:]]*ignore
#test: value = parse(raw)  # type: ignore[arg-type]
[#][[:space:]]*pragma:[[:space:]]*no cover
#test: def debug_dump():  # pragma: no cover
pylint:[[:space:]]*disable
#test: # pylint: disable=broad-except
[#]pragma[[:space:]]+warning[[:space:]]+disable
#test: #pragma warning disable CS8618
SuppressMessage\(
#test: [SuppressMessage("Design", "CA1031")]
NOSONAR
#test: catch (Exception) { throw; } // NOSONAR
ExcludeFromCodeCoverage
#test: [ExcludeFromCodeCoverage]
//[[:space:]]*nolint
#test: if err := run(); err != nil { //nolint:errcheck
@SuppressWarnings
#test: @SuppressWarnings("unchecked")
@Suppress\(
#test: @Suppress("UNCHECKED_CAST")
[#]!?\[allow\(
#test: #[allow(dead_code)]
nosemgrep
#test: eval(userInput) // nosemgrep
gitleaks:allow
#test: const sample = "abc"; // gitleaks:allow
(^|[^A-Za-z0-9_])nosec([^A-Za-z0-9_]|$)
#test: subprocess.call(cmd, shell=True)  # nosec
Stryker disable
#test: // Stryker disable next-line all
#test-clean: // @ts-expect-error: the upstream type is wrong, tracked upstream
#test-clean: const config = loadEslintConfig(root);
#test-clean: # the type annotation is ignored by the serializer
#test-clean: [JsonIgnore]
#test-clean: const nosecret = readVault();

# rule: test-made-easier
(^|[^A-Za-z0-9_])(it|test|describe|context|suite)\.(skip|only)\(
#test: it.skip('rejects an expired token', async () => {
#test: describe.only('checkout', () => {
(^|[^A-Za-z0-9_.])x(it|describe|test)\(
#test: xit('rounds half-cents up', () => {
@pytest\.mark\.skip
#test: @pytest.mark.skip(reason="flaky on CI")
(^|[^A-Za-z0-9_])t\.Skip(Now|f)?\(
#test: 	t.Skip("needs a database")
\[(Fact|Theory)\([^]]*Skip[[:space:]]*=
#test: [Fact(Skip = "broken after the refactor")]
\[Ignore(\(|\])
#test: [Ignore("revisit")]
@(Disabled|Ignore)([^A-Za-z0-9_]|$)
#test: @Disabled
#test-clean: return events.skip(1).take(3);
#test-clean: var page = query.Skip(20).Take(10);
#test-clean: [JsonIgnore(Condition = JsonIgnoreCondition.Always)]
#test-clean: it('rejects an expired token', async () => {
#test-clean: const exit = await shell.exec('npm test');

# rule: unfinished-work
throw[[:space:]]+new[[:space:]]+NotImplementedException
#test: public Task Handle() => throw new NotImplementedException();
throw[[:space:]]+new[[:space:]]+Error\([^)]*[Nn]ot[[:space:]]+implemented
#test: throw new Error("not implemented yet");
(^|[^A-Za-z0-9_])(todo|unimplemented)!\(
#test: fn settle(&self) -> Amount { todo!() }
panic\([^)]*[Nn]ot[[:space:]]+implemented
#test: panic("not implemented")
catch[[:space:]]*(\([^)]*\))?[[:space:]]*\{[[:space:]]*\}
#test: try { save(); } catch (e) {}
#test: } catch {}
except[^:]*:[[:space:]]*pass([[:space:]]|$)
#test: except Exception: pass
\.catch\([[:space:]]*\([^)]*\)[[:space:]]*=>[[:space:]]*\{[[:space:]]*\}[[:space:]]*\)
#test: fetchRates().catch(() => {});
#test-clean: catch (err) { logger.error(err); throw err; }
#test-clean: raise NotImplementedError  # abstract: every provider overrides this
#test-clean: // TODO(ENG-441): tighten once the migration lands
#test-clean: const implemented = features.filter(f => f.done);
